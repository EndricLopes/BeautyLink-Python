{
    "version": 2,
    "builds": [
        {
            "src": "server-flask/server.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "./(.*)",
            "dest": "server-flask/server.py"
        }
    ]
}